name: Update FAA navaids

on:
  workflow_dispatch:

  schedule:
    - cron: "37 9 * * 1"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Update FAA navaid snapshot
        run: python scripts/update_navaids.py

      - name: Commit updated snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add navaids_snapshot.json

          if git diff --cached --quiet; then
            echo "No navaid changes."
            exit 0
          fi

          git commit -m "Update FAA navaid snapshot"
          git push
